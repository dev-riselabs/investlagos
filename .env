VITE_API_URL=https://api.investlagos.org/api

# Site-wide maintenance toggle. When set to "true", every public route
# redirects to /maintenance. Admin routes (/admin/*) remain accessible.
# Restart `npm run dev` (or rebuild) after changing this value.
VITE_MAINTENANCE_MODE=true
